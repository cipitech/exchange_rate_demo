package com.cipitech.tools.converters.exchange.client.api;

/**
 * Represents a datasource fetcher.
 * A fetcher can fetch ONE type of information from its source (e.g. local file system, API endpoint etc.)
 * For every different type you must extend this interface.
 */
public interface AbstractFetcher
{
}
